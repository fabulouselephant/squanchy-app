import { Typography, Stack } from "@mui/material"
import { styled } from '@mui/material/styles'

export const CharachterDescripionLine = styled(Stack)({
    fontFamily: 'PTSans', 
    fontSize: '16px'
})